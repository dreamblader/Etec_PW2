@page
@model site.Pages.ReviseModel
@{
    ViewData["Title"] = "Review Page";
}

<div class="row">
@foreach (Local local in Model.list)
{
    <div class="col-4 p-2">
        <a class="row" href="/LocalDetail?id=@local.id">
            <div class="col-6">
                <img class="img-thumbnail" src=@local.imagem />
            </div>
            <div class="col-6">
                <h5>@local.nome</h5>
                <h6>@local.pais - @local.cidade</h6>
                <div class="btn btn-primary justify-content-center mt-2">Ver Mais</div>
            </div>
            
        </a>
    </div>
}
</div>